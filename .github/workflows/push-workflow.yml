name: Push Workflow

on:
  push:
    branches:
    - main

jobs:
  gcp_workflow:
    uses: ./.github/workflows/gcp-workflow.yml
    with:
      WORKING_DIRECTORY: "./gcp"
    secrets: inherit
  aws_workflow:
    uses: ./.github/workflows/gcp-workflow.yml
    with:
      WORKING_DIRECTORY: "./aws"
    secrets: inherit
